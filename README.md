# 投资研究作品集

WorkBuddy 生成的研究报告合集，公开分享版，部署于 GitHub Pages（不依赖 WorkBuddy）。

- `index.html`：导航页入口
- 各子目录：单篇报告

## 如何更新
1. 新增/修改报告文件，或编辑 `index.html` 里的卡片
2. `git add -A && git commit -m "更新说明" && git push`
3. GitHub Pages 自动重建（约 1 分钟生效）
